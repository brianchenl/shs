#!/bin/sh

filename=$1
echo "--------------------------------------------------------------------------------"
ls -l ${filename}
ls ${filename} | sed "s:^:`pwd`/: "
md5sum ${filename} | awk '{print $1"\t"$2"\t"}' > ~/md5.txt
cat ~/md5.txt

echo "--------------------------------------------------------------------------------"
